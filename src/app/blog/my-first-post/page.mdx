import metadataJson from './metadata.json'
import BlogLayout from '@/app/blog/blog-layout.tsx'
import { MdxImage } from '../../../components/mdx-image'
import Img1 from './image-1.jpeg'
import Img2 from './image-2.jpeg'
import Img3 from './image-3.jpeg'
import Img4 from './image-4.jpeg'
import Link from 'next/link'

export const AComponent = () => {
    return <strong>JSX!</strong>
}

Hello world, this is my first post.

And, it works with <AComponent />

Have a look at these photos I took to celebrate the occasion 👇

<div
    className={
        'my-4 grid grid-cols-1 grid-rows-4 gap-4 md:grid-cols-3 md:grid-rows-2'
    }
>
    <div className={'relative h-52'}>
        <Link
            href={Img1.src}
            target={'_blank'}
        >
            <MdxImage
                src={Img1}
                alt={
                    "Inspirational words from someone called T. (Swift? I'm kidding.)"
                }
                fill={true}
                className={'rounded-lg'}
                placeholder={'blur'}
                style={{ objectFit: 'cover', objectPosition: 'center' }}
            />
        </Link>
    </div>
    <div className={'relative h-52 md:col-span-2'}>
        <Link
            href={Img2.src}
            target={'_blank'}
        >
            <MdxImage
                src={Img2}
                alt={`A pretty cool wide shot that's kinda neat I think`}
                fill={true}
                className={'rounded-lg'}
                placeholder={'blur'}
                style={{ objectFit: 'cover', objectPosition: 'center' }}
            />
        </Link>
    </div>
    <div className={'relative h-52 md:col-span-2'}>
        <Link
            href={Img4.src}
            target={'_blank'}
        >
            <MdxImage
                src={Img4}
                alt={'Check out this cloud, looks like a dolphin...'}
                fill={true}
                className={'rounded-lg'}
                placeholder={'blur'}
                style={{ objectFit: 'cover', objectPosition: 'center' }}
            />
        </Link>
    </div>
    <div className={'relative h-52'}>
        <Link
            href={Img3.src}
            target={'_blank'}
        >
            <MdxImage
                src={Img3}
                alt={'Check out this cloud, looks like a dolphin...'}
                fill={true}
                className={'rounded-lg'}
                placeholder={'blur'}
                style={{ objectFit: 'cover', objectPosition: 'center' }}
            />
        </Link>
    </div>
</div>

And that's all.

export const metadata = metadataJson

export default function MDXPage({ children }) {
    return <BlogLayout blogPostId={metadata.id}>{children}</BlogLayout>
}
